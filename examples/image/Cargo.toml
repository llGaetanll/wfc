[package]
name = "image"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# sdl2 = { version = "0.35.2", features = ["image"], default-features = false }
wfc = { path = "../../" }
image = "0.25.0"

[[bin]]
name = "image"
path = "main.rs"
